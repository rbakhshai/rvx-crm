/**
 * Default render of the @drawer parallel slot — nothing.
 * Intercepted routes under @drawer/(.)... will override this when active.
 */
export default function DrawerDefault() {
  return null;
}
