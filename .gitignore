node_modules/
.next/
out/
build/
dist/
tsconfig.tsbuildinfo

# env
.env
.env.local
.env*.local

# logs
*.log
npm-debug.log*

# editor
.vscode/
.idea/
.DS_Store

# drizzle — generated migrations are versioned source of truth (run on deploy).
# (Previously ignored when we were push-only; now tracked.)

# vercel
.vercel

# claude code per-user settings
.claude/

# Ontraport migration dumps — contain client PII (emails, phones, notes).
# Keep the snapshot doc, ignore the raw exports.
raw/*.json

# DD worksheet templates — proprietary internal docs, not in source control
*.xlsx
